#ifndef IMAGE_UTILS_H
#define IMAGE_UTILS_H
#include <vector>
#include <QString>
#include <map>


class image_utils
{
public:
    image_utils();
    inline static unsigned npos;
    inline static std::map<std::string, bool> result;
    static std::string get_error();
};

#endif // IMAGE_UTILS_H
